'use strict'

/*class CRUD {
  constructor(){
    this._array = []
  }

  add(o){
    this._array.push(o)
  }

  constaines(o){
    return typeof this._array.find(({ id }) => o.id === id) !== 'undefined'
  }

  update(o){
    this._array.forEach((a,i,arr) => {
      if(o.id === arr[i].id)
      arr[i] = o
    })
  }

  delete(o){
    this._array = this._array.filter(({ id }) => o.id !== id)
  }

  get(){
    return this._array.slice(0)
  }
}*/